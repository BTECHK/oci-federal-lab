# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.eggs/
dist/
build/
venv/
.venv/

# Terraform
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
*.tfvars
!*.tfvars.example
*.tfstate.backup

# Ansible
*.retry

# Secrets & credentials
.env
.env.*
!.env.example
credentials/
credentials.json
*-credentials.json
service-account*.json
*.pem
*.key
ansible-vault-password
vault-password.txt
*/github/oci-federal-lab/auth
# SSH keys
id_rsa*
*.pub

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Docker
*.log

# Data files
*.db
*.csv

# Large binary files
*.pdf

# Large generated reports (OpenSCAP, etc.)
**/screenshots/*.html

# Claude Code / planning
.claude_session_context.md
.claude/
.planning/

# Temp files
nul
*.tmp
*.bak

# OCI audit logs and error dumps
logs and errors/
logs-and-errors/

# Continue-here planning file
.continue-here.md

# Local-only workflow/coordination files (not for public repo)
CLAUDE.md
CHANGES-STAGING.md

# Phases not yet in progress — hidden from public view until work begins.
# Files remain on disk locally; they're just untracked. When a phase starts,
# remove its entry here to bring it back into the tracked tree.

# 4-quadrant scaffold/answers pattern (gitignore answers, breaks, solutions everywhere)
**/answers/
**/breaks/
**/solutions/

# Generated gRPC stubs — produced by fedagent/proto/generate.sh
fedagent/pb/
fedtracker-app/proto/*_pb2.py
fedtracker-app/proto/*_pb2_grpc.py
fedtracker-app/proto/*_pb2.pyi

# Go local build outputs
fedagent/fedagent
fedagent/fedagent.exe
*.exe

# --- Local agent tooling & knowledge base — kept local, not part of this portfolio repo ---
.agents/
.claude/skills/
.gitnexus/
skills-lock.json
AGENTS.md
learnings.md
wiki/
raw/
